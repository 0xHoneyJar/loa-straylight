# Freeside community / app surface mapping — Straylight primitives to Freeside surfaces

> Status: Phase 14. **Pre-integration handoff packet, in
> `loa-straylight` only.** This table maps each Straylight
> primitive / operation to the proposed Freeside community / bot /
> admin / tenant / Discord / Telegram / REST / NATS surface, the
> required input, the required output, the fail-closed condition,
> the receipt / provenance requirement, the related Hounfour
> schema candidate, the related Finn enforcement point, the
> related Dixie BFF surface, and the notes that pin the boundary.
> **Filing this against `loa-freeside` is not part of Phase 14.**
> Nothing here imports from `loa-freeside`, edits any sibling
> repo, or changes Phase 0–12 runtime behavior.

## How to read this table

| Column | Meaning |
|---|---|
| **Straylight primitive / operation** | The wedge primitive (or the community-level operation built over it) the row covers. |
| **Current Straylight local source** | Where the wedge implements this primitive today. |
| **Future Freeside community / app surface** | The Freeside-side surface that should expose this primitive after consuming stable contracts. |
| **Required input** | The shape Freeside must accept and pass to the wedge / Finn / Dixie. |
| **Required output** | The shape Freeside must produce on success (bot reply, REST response, admin dashboard render, capability grant receipt). |
| **Fail-closed condition** | What Freeside must do when the surface cannot complete the operation, the receipt is missing, the chain is broken, or the privacy / tenant boundary is violated. |
| **Receipt / provenance requirement** | The receipt(s) Freeside MUST surface alongside the rendered output, and the provenance refs the community surface MUST honor. |
| **Related Hounfour schema candidate** | The schema id Freeside consumes for the shape. `—` if no Hounfour schema is the load-bearing input. |
| **Related Finn enforcement point** | The Finn runtime gate that Freeside's community surface sits in front of. `—` if Freeside consumes the wedge directly (in single-process deployments). |
| **Related Dixie BFF surface** | The Dixie BFF / inspection surface that Freeside calls for operator-facing semantics. `—` if no Dixie surface is required. |
| **Notes** | Constraints, architecture-spec citations, and "stays in Straylight / Finn / Dixie" runtime references. |

## Mapping

### Discord / Telegram bot recall

| Straylight primitive / operation | Current Straylight local source | Future Freeside community / app surface | Required input | Required output | Fail-closed condition | Receipt / provenance requirement | Related Hounfour schema candidate | Related Finn enforcement point | Related Dixie BFF surface | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| `bot_recall_request` (Discord / Telegram) | [`src/straylight/validators/class-validator.ts`](../../src/straylight/validators/class-validator.ts) (`validateRecallRequest`) + [`src/straylight/policy.ts`](../../src/straylight/policy.ts) (`policyForRecallRequest`) | Discord / Telegram bot adapter ingress (`discord:bot.recall`, `telegram:bot.recall`). Freeside builds a `RecallRequest` envelope from the platform event, attaches the platform's `EnvironmentFrame`, runs tenant-boundary checks, and forwards to the runtime gate (via Dixie or Finn). | Platform event (Discord message / Telegram message), caller signer envelope, target `actor_id` / `estate_id`, platform tenant id. | A `RecallRequest` envelope (per the wedge's `validateRecallRequest` shape) plus an intake decision (`accepted` / `denied_intake`). | Class validation fails, signer envelope malformed, `environment_frame` unknown, or tenant-boundary check fails → deny intake; emit Freeside-side audit log entry; never forward to runtime gate. | Intake response carries the request envelope (no receipt yet). The receipt is emitted by the runtime gate on `recall_pack_generation`. | `straylight.recall_request.v0`, `straylight.environment_frame.v0`, `straylight.risk_level.v0` | `recall_request` (per Finn mapping). | `recall_request` intake (per Dixie mapping). | Per [arch spec §22.7.G1](../architecture/loa-straylight-product-system-architecture-spec.md). Cross-tenant prevention is a Freeside-layer responsibility (first line of defense); Dixie / Finn / the wedge are the second / third / fourth line. |
| `bot_recall_response` (Discord / Telegram) | [`src/straylight/recall.ts`](../../src/straylight/recall.ts) (`executeRecall` interior) | Discord / Telegram bot reply. Freeside renders the served `RecallPack` + `RecallReceipt` in the bot's response shape — included items as quotable, marked items in a "contested" / "demoted" UI affordance, excluded items as a public summary. | `recall_request_id`. | A bot reply containing the served `RecallPack` (with `included` / `marked` / `redacted` discipline) plus the matching `RecallReceipt` reference. | Receipt missing, stale, or `pack_hash` does not match the served pack → refuse to render; surface deny reason. A pack without a receipt is treated as a denied recall. | The `RecallPack` MUST be paired with its `RecallReceipt`; the bot adapter MUST NOT render the pack alone. Provenance refs travel via `included[].provenance_refs`. | `straylight.recall_pack.v0`, `straylight.recall_item.v0`, `straylight.recall_use_instruction.v0`, `straylight.redaction_summary.v0`, `straylight.exclusion_summary.v0` | `recall_pack_generation` (per Finn mapping). | `recall_pack_generation` response (per Dixie mapping). | Per [arch spec §22.7.G1](../architecture/loa-straylight-product-system-architecture-spec.md). The `included` / `marked` / `redacted` discipline is load-bearing — `marked` items are never quotable as fact in a bot reply. |

### Tenant-admin and community-scoped recall

| Straylight primitive / operation | Current Straylight local source | Future Freeside community / app surface | Required input | Required output | Fail-closed condition | Receipt / provenance requirement | Related Hounfour schema candidate | Related Finn enforcement point | Related Dixie BFF surface | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| `tenant_scoped_recall` | [`src/straylight/policy.ts`](../../src/straylight/policy.ts) (signer-competence + estate scoping) | `POST /admin/recall` (Freeside REST) and tenant-admin Discord / Telegram channels. Freeside resolves the caller's tenant and forwards. | Caller tenant id, target `actor_id` / `estate_id`, `RecallRequest` envelope. | A served `RecallPack` + `RecallReceipt` scoped to the caller's tenant. | Tenant boundary undefined or unmatched → deny intake; never default-allow. | Receipt MUST be surfaced; provenance walks respect tenant scope. | `straylight.recall_request.v0`, `straylight.actor.v0`, `straylight.actor_estate.v0` | `recall_request` (per Finn mapping). | `recall_request` intake + `recall_pack_generation` (per Dixie mapping). | Per [arch spec §16.1, §22.7](../architecture/loa-straylight-product-system-architecture-spec.md). Tenant boundary holds at every layer; Freeside is the first line. |
| `community_scoped_recall` | [`src/straylight/policy.ts`](../../src/straylight/policy.ts) (recall scope filters) | `GET /community/:community_id/recall` and community-side Discord / Telegram channels. Freeside resolves community context and applies community-side scope filters. | Caller community id, target `actor_id` / `estate_id`, `RecallRequest` envelope. | A served `RecallPack` + `RecallReceipt` scoped to the community's recall scope. | Community context unresolved → treat as non-admin community-member access; never default to admin scope. | Receipt MUST be surfaced; community-scoped provenance only. | `straylight.recall_request.v0`, `straylight.recall_pack.v0` | `recall_request` (per Finn mapping). | `recall_pack_generation` (per Dixie mapping). | Per [arch spec §22.7](../architecture/loa-straylight-product-system-architecture-spec.md). Community scope is a Freeside-side overlay on top of the wedge's tenant boundary. |

### Public-channel redaction

| Straylight primitive / operation | Current Straylight local source | Future Freeside community / app surface | Required input | Required output | Fail-closed condition | Receipt / provenance requirement | Related Hounfour schema candidate | Related Finn enforcement point | Related Dixie BFF surface | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| `public_channel_redaction` | [`src/straylight/recall.ts`](../../src/straylight/recall.ts) (`privacyDispositionForFrame`) | Middleware on every `public_discord` / `public_telegram` / `repo_workflow` surface. Freeside honors the wedge / Finn / Dixie's exclusion summary; bodies do not travel. | Caller frame (`public_discord` / `public_telegram` / `repo_workflow`), wedge / Finn / Dixie's `RecallPack`. | Bot reply with included / marked items (bodies for public-scope only) and exclusion summary entries (counts, reasons; never bodies). | Public channel that would have to synthesize a private body to answer the prompt → return the exclusion summary; do **not** answer the prompt. Frame elevation forbidden. | Receipt MUST accompany every served pack. Bodies on `actor_private` / `sealed` material NEVER travel. | `straylight.environment_frame.v0`, `straylight.privacy_scope.v0`, `straylight.exclusion_summary.v0` | `recall_pack_generation` (the runtime gate's `dispositionFor` step). | `excluded_assertion_reason_display` (per Dixie mapping). | Per [arch spec §6.2.5](../architecture/loa-straylight-product-system-architecture-spec.md), [`threat-model.md`](../mvp/threat-model.md) T1. The wedge's `privacyDispositionForFrame` is the source of truth; Freeside's renderer does not derive its own privacy decisions. |

### Discord / Telegram environment-frame routing

| Straylight primitive / operation | Current Straylight local source | Future Freeside community / app surface | Required input | Required output | Fail-closed condition | Receipt / provenance requirement | Related Hounfour schema candidate | Related Finn enforcement point | Related Dixie BFF surface | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| `discord_environment_frame_routing` | [`src/straylight/types.ts`](../../src/straylight/types.ts) (`EnvironmentFrame` enum) | Discord adapter's frame attach. Public channel → `public_discord`; private DM → `private_chat`; admin channel (with role check) → `private_operator` (caller frame intent `tenant_admin`); audit-review channel (gated by reviewer signer) → `audit_review` (forwarded to Dixie). | Discord platform context (guild id, channel id, role check), caller signer envelope. | A `RecallRequest` (or other governed call) tagged with the appropriate `EnvironmentFrame`. | Discord context unknown / spoofed → deny intake with `unknown_environment_frame`. Frame elevation forbidden. | Frame travels with every recall request; the wedge / Finn / Dixie return frame-appropriate output. | `straylight.environment_frame.v0` | `recall_request` (per Finn mapping). | `public_private_environment_frame_handling` (per Dixie mapping). | Per [arch spec §22.7.G1](../architecture/loa-straylight-product-system-architecture-spec.md). Freeside-side frame intents (`tenant_admin`, `community_dashboard`) are presentation overlays; they map back to existing wedge frames. |
| `telegram_environment_frame_routing` | [`src/straylight/types.ts`](../../src/straylight/types.ts) (`EnvironmentFrame` enum) | Telegram adapter's frame attach. Public chat → `public_telegram`; private DM → `private_chat`; admin chat (with role check) → `private_operator` (caller frame intent `tenant_admin`); audit-review surfaces NOT exposed via Telegram. | Telegram platform context (chat id, admin status), caller signer envelope. | A `RecallRequest` (or other governed call) tagged with the appropriate `EnvironmentFrame`. | Telegram context unknown / spoofed → deny intake with `unknown_environment_frame`. Frame elevation forbidden. | Frame travels with every recall request; the wedge / Finn / Dixie return frame-appropriate output. | `straylight.environment_frame.v0` | `recall_request` (per Finn mapping). | `public_private_environment_frame_handling` (per Dixie mapping). | Per [arch spec §22.7.G1](../architecture/loa-straylight-product-system-architecture-spec.md). Audit-review surfaces are not exposed via Telegram — they live in Dixie. |

### REST / NATS surface routing

| Straylight primitive / operation | Current Straylight local source | Future Freeside community / app surface | Required input | Required output | Fail-closed condition | Receipt / provenance requirement | Related Hounfour schema candidate | Related Finn enforcement point | Related Dixie BFF surface | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| `rest_api_recall_routing` | [`src/straylight/types.ts`](../../src/straylight/types.ts) (`EnvironmentFrame` enum) + [`src/straylight/policy.ts`](../../src/straylight/policy.ts) (signer competence) | REST API routes (e.g. `POST /api/v1/recall`, `POST /api/v1/admin/...`). Freeside attaches the appropriate `EnvironmentFrame` (`private_operator`, `repo_workflow`) and tenant boundary check. | Tenant header, caller signature, route prefix, request body. | A frame-tagged, tenant-scoped governed call forwarded to the runtime gate. | Route or NATS subject not on the allowlist for the caller's tenant → deny; never default-allow. Public REST endpoints MUST NOT serve governed recall. | Frame + receipt travel with every governed call; receipt persistence is the runtime gate's responsibility. | `straylight.environment_frame.v0`, `straylight.signer_competence_result.v0` | `recall_request` / per-transition gates (per Finn mapping). | `public_private_environment_frame_handling` (per Dixie mapping). | Per [arch spec §22.7](../architecture/loa-straylight-product-system-architecture-spec.md). REST and NATS surfaces are Freeside-internal; the wedge / Finn / Dixie do not prescribe their shape. |

### Feedback signal capture

| Straylight primitive / operation | Current Straylight local source | Future Freeside community / app surface | Required input | Required output | Fail-closed condition | Receipt / provenance requirement | Related Hounfour schema candidate | Related Finn enforcement point | Related Dixie BFF surface | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| `feedback_signal_capture` | [`src/straylight/validators/class-validator.ts`](../../src/straylight/validators/class-validator.ts) (`validateCandidateAssertion`) + [`src/straylight/policy.ts`](../../src/straylight/policy.ts) (`policyForAdmitAssertion`) | Discord / Telegram / NATS / REST event ingest (`discord:event.ingest`, `telegram:event.ingest`, `nats:event.ingest`). Freeside builds a candidate `feedback_signal` (or platform-supplied class) per Hounfour's class shape, fills `provenance[]`, and hands the candidate to Finn's admission gate. | Platform event payload, bot / community-runtime signer envelope, platform event reference (for `provenance.captured_by` / `evidence_summary`). | A `CandidateAssertion` with `provenance[].source_type` set to `community_event` / `social_signal` / `runtime_observation`, plus the admission decision (`accepted` / `denied` / `marked` / `needs_review`). | Class shape, signature, or provenance fails validation → deny intake; emit Freeside-side audit log entry; never admit as memory truth. | Admission decision MUST emit a `transition_receipt` + audit event; Freeside surfaces both. | `straylight.candidate_assertion.v0`, `straylight.assertion_class.v0`, `straylight.provenance_ref.v0`, `straylight.provenance_source_type.v0` | `classify_assertion` / `admit_assertion` (per Finn mapping). | `assertion_status_inspection` (post-admission, per Dixie mapping). | Per [arch spec §22.7.G2](../architecture/loa-straylight-product-system-architecture-spec.md). Discord / Telegram / social / economic feedback signals enter as candidate `feedback_signal`, not as memory truth. The platform event is provenance, not authority. |

### Admin capability grants

| Straylight primitive / operation | Current Straylight local source | Future Freeside community / app surface | Required input | Required output | Fail-closed condition | Receipt / provenance requirement | Related Hounfour schema candidate | Related Finn enforcement point | Related Dixie BFF surface | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| `admin_capability_grant` | [`src/straylight/policy.ts`](../../src/straylight/policy.ts) (`policyForTransition`) + [`src/straylight/keyring.ts`](../../src/straylight/keyring.ts) (`evaluateCompetence`) | `POST /admin/capability/grant` (Freeside REST) and admin Discord / Telegram channels. Freeside builds a candidate `permission` assertion, verifies the admin's role + competence, and hands the candidate to Finn's transition gate. | Admin signer envelope, target `actor_id` / `estate_id`, scoped capability description, requested-effect, signature. | A `permission` transition receipt + audit event on allow; deny reason on deny; `needs_review` decision on quorum / timelock / human-review. | Capability grant attempted by a signer who does not pass `evaluateCompetence` → deny; `signer_role_forbidden:<role>`. `needs_review` MUST hold; never auto-promote. | The admin grant emits `permission_granted` audit event + `transition_receipt`; Freeside surfaces both. | `straylight.assertion.v0`, `straylight.transition_receipt.v0`, `straylight.policy_decision.v0`, `straylight.signer_competence_result.v0` | `classify_assertion` / `admit_assertion` (`permission` class) (per Finn mapping). | `assertion_status_inspection` (per Dixie mapping). | Per [arch spec §22.7.G3](../architecture/loa-straylight-product-system-architecture-spec.md). Community admins can grant scoped bot capability with policy and audit; bypassing the gate (e.g. via a Freeside-internal capability table) re-creates the ungoverned-permission failure mode. |

### Bot action with receipt

| Straylight primitive / operation | Current Straylight local source | Future Freeside community / app surface | Required input | Required output | Fail-closed condition | Receipt / provenance requirement | Related Hounfour schema candidate | Related Finn enforcement point | Related Dixie BFF surface | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| `bot_action_with_receipt` | [`src/straylight/policy.ts`](../../src/straylight/policy.ts) (`policyForTransition`) + [`src/straylight/estate.ts`](../../src/straylight/estate.ts) (transition execution) | Discord / Telegram / NATS / REST bot action (`discord:bot.action`, `telegram:bot.action`, `nats:bot.action`, `POST /api/v1/bot/action`). Freeside builds a candidate `action` assertion with the bot's signer envelope and hands the candidate to Finn's transition gate. | Bot signer envelope, target `actor_id` / `estate_id`, action description, requested-effect, signature. | An `action` transition receipt + audit event on allow; deny reason on deny; `needs_review` decision on quorum / timelock / human-review. | Bot action without a competent signer, without a passing policy decision, or without a persisted receipt → do not execute; do not pretend to execute. | Every action emits `action_executed` audit event + `transition_receipt`; Freeside surfaces both alongside the action result. | `straylight.assertion.v0`, `straylight.transition_receipt.v0`, `straylight.policy_decision.v0` | `classify_assertion` / `admit_assertion` (action class) (per Finn mapping). | `assertion_status_inspection` (per Dixie mapping). | Per [arch spec §6.2.5](../architecture/loa-straylight-product-system-architecture-spec.md). The wedge's promotion chain (memory → belief → instruction → plan → permission → action → commitment → permanence) holds through Freeside; the bot does not get to skip policy validation. |

### Cross-tenant recall prevention

| Straylight primitive / operation | Current Straylight local source | Future Freeside community / app surface | Required input | Required output | Fail-closed condition | Receipt / provenance requirement | Related Hounfour schema candidate | Related Finn enforcement point | Related Dixie BFF surface | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| `cross_tenant_recall_prevention` | [`src/straylight/policy.ts`](../../src/straylight/policy.ts) (signer-competence + estate scoping); [`src/straylight/audit.ts`](../../src/straylight/audit.ts) (per-estate chain — cross-estate links forbidden) | Middleware on every Freeside surface (Discord guild check, Telegram chat check, REST API key check, NATS subject check). Freeside enforces tenant boundary at ingress; Dixie / Finn / the wedge enforce as second / third / fourth line of defense. | Caller session (tenant id), target `actor_id` / `estate_id`, requested operation. | Accept (forward to runtime gate) or deny (`cross_tenant_recall_refused`). | Tenant boundary check fails → deny intake; emit Freeside-side audit log entry referencing the wedge's `recall_denied`. Tenant boundary undefined → deny intake; never default-allow. | The intake-deny audit log entry references the wedge / Finn's `recall_denied` event for chain-of-custody. | `straylight.actor.v0`, `straylight.actor_estate.v0` | All tenant-scoped Finn enforcement points (every row in [`finn-enforcement-mapping.md`](./finn-enforcement-mapping.md) that operates on an estate). | `cross_tenant_recall_prevention` (per Dixie mapping). | Per [arch spec §16.1, §22.7](../architecture/loa-straylight-product-system-architecture-spec.md), [`threat-model.md`](../mvp/threat-model.md) T6. Freeside is the **first** line of defense; Dixie / Finn / the wedge are the second / third / fourth. Every layer must refuse. |

### Challenged / revoked / forgotten awareness in community surfaces

| Straylight primitive / operation | Current Straylight local source | Future Freeside community / app surface | Required input | Required output | Fail-closed condition | Receipt / provenance requirement | Related Hounfour schema candidate | Related Finn enforcement point | Related Dixie BFF surface | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| `challenge_revocation_awareness_community` | [`src/straylight/types.ts`](../../src/straylight/types.ts) (`Challenge`, `Revocation`, `ForgetRecord`); [`src/straylight/recall.ts`](../../src/straylight/recall.ts) (`dispositionFor`) | Cross-cutting: every bot reply, REST response, and admin dashboard MUST consult `Challenge` / `Revocation` / `ForgetRecord` records before rendering an assertion as `active`. Public channels render a redacted notice; admin dashboards render the timeline. | `assertion_id`, caller's `environment_frame`. | A bot / REST / admin response that surfaces the challenge / revocation / forget record (or a redacted notice in public channels). | Governance record unparseable → render as `excluded` with reason `unparseable_governance_record`; never as `active`. | Governance records emit transition receipts (challenge / revoke / forget transitions); Freeside surfaces both. | `straylight.challenge.v0`, `straylight.revocation.v0`, `straylight.forget_record.v0`, `straylight.assertion_status.v0` | `challenge_assertion` / `revoke_assertion` / `forget_assertion_from_recall` (per Finn mapping). | `challenge_revocation_awareness` / `assertion_status_inspection` (per Dixie mapping). | Per [arch spec §6.2.5, §22.7](../architecture/loa-straylight-product-system-architecture-spec.md). Challenge / revocation / forget do not erase; the community surface preserves both halves. |

### Tenant-admin estate inspection

| Straylight primitive / operation | Current Straylight local source | Future Freeside community / app surface | Required input | Required output | Fail-closed condition | Receipt / provenance requirement | Related Hounfour schema candidate | Related Finn enforcement point | Related Dixie BFF surface | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| `tenant_admin_estate_inspection` | [`src/straylight/types.ts`](../../src/straylight/types.ts) (`Actor`, `ActorEstate`, `Keyring`); [`src/straylight/audit.ts`](../../src/straylight/audit.ts) (`AuditLog.list`, `verifyChain`) | `GET /admin/estate/:estate_id/...` and admin Discord / Telegram channels. Freeside forwards inspection requests to Dixie's inspection surface with the caller's tenant context; renders the response respecting the admin's role. | Admin caller, target `estate_id`, inspection kind (summary / audit / status). | Admin-scoped inspection view: counts, audit chain head, assertion statuses (no `actor_private` bodies unless the admin holds the reviewer signer). | Inspection that would expose `actor_private` body to a community admin → refuse; surface count, not body. | Admin inspection does not emit a new receipt; it surfaces the existing transition / recall receipts the wedge / Finn / Dixie already wrote. | `straylight.actor.v0`, `straylight.actor_estate.v0`, `straylight.keyring.v0`, `straylight.audit_event.v0` | State-of-record reads (per Finn mapping). | `actor_estate_summary` / `audit_chain_lookup` / `assertion_status_inspection` (per Dixie mapping). | Per [arch spec §22.7](../architecture/loa-straylight-product-system-architecture-spec.md). Community-admin role lets the admin view counts, not bodies; bodies stay private unless the admin holds the reviewer signer key (in which case `audit_review` applies and Dixie hosts the surface). |

## Cross-references

- [`docs/handoffs/freeside-community-surface-issue.md`](./freeside-community-surface-issue.md)
  — Phase 14 issue handoff that consumes this mapping.
- [`docs/handoffs/freeside-community-surface-boundary.md`](./freeside-community-surface-boundary.md)
  — companion boundary doc (what Freeside owns vs what it
  must not own).
- [`docs/handoffs/dixie-recall-mapping.md`](./dixie-recall-mapping.md)
  — Phase 12 mapping from Straylight primitives / operations
  to Dixie BFF / inspection surfaces.
- [`docs/handoffs/finn-enforcement-mapping.md`](./finn-enforcement-mapping.md)
  — Phase 10 mapping from Straylight transitions to Finn
  runtime enforcement points (runtime lane).
- [`docs/handoffs/hounfour-extraction-mapping.md`](./hounfour-extraction-mapping.md)
  — Phase 9 mapping from Straylight primitives to Hounfour
  schema candidates (class lane).
- [`docs/architecture/loa-straylight-product-system-architecture-spec.md`](../architecture/loa-straylight-product-system-architecture-spec.md)
  §6.2.5, §16.1, §16.2, §22.7 — architectural decisions.
- [`docs/mvp/package-boundary.md`](../mvp/package-boundary.md) —
  the wedge's stable public API surface.
- [`docs/mvp/threat-model.md`](../mvp/threat-model.md) — the
  fail-closed defenses the community surface must preserve.
- [`fixtures/freeside-community-surface/`](../../fixtures/freeside-community-surface/)
  — ten current-shape JSON examples (Freeside PR-A test
  inputs).
